package com.real.fudousan.memberfile.service;

import org.springframework.stereotype.Service;

@Service
public class MemberFileService {

	/**
	 * 파일 추가
	 * @param memberId
	 * @param fileName
	 * @param data
	 * @return
	 */
	public int upload(int memberId, String title, String text, String fileName, Byte[] data) {
		
		return -1;
	}
}
